import type { Config } from "tailwindcss";

const config: Config = {
    darkMode: "class",
    content: [
        "./src/pages/**/*.{js,ts,jsx,tsx,mdx}",
        "./src/components/**/*.{js,ts,jsx,tsx,mdx}",
        "./src/app/**/*.{js,ts,jsx,tsx,mdx}",
    ],
    theme: {
        extend: {
            colors: {
                "navy": {
                    900: "#0f172a", // Deepest Navy/Slate
                    800: "#1e3a8a", // Classic Navy
                    700: "#1d4ed8",
                },
                "royal": {
                    600: "#2563eb", // Royal Blue
                    500: "#3b82f6",
                },
                "primary": "#1e3a8a", // Navy 800 - Default for landing
                // "primary" is reused in builder with a different blue (#1152d4), 
                // I will add a specific one for apps if needed or override in usage.
                // The user provided config has `primary` as `#1e3a8a` in landing and `#1152d4` in builder.
                // I'll add both variants.
                "landing-primary": "#1e3a8a",
                "landing-primary-hover": "#172554",
                "app-primary": "#1152d4", // The brighter blue for the app

                "accent": "#2563eb", // Royal Blue
                "background-light": "#ffffff", // Landing default
                "background-dark": "#020617", // Landing default
                "app-bg-light": "#f6f6f8", // Builder/Analyzer default
                "app-bg-dark": "#101622", // Builder/Analyzer default

                "text-main": "#0f172a",
                "text-sub": "#475569", // Slate 600
                "border-light": "#e2e8f0",
                "paper": "#ffffff",
            },
            fontFamily: {
                "display": ["Inter", "sans-serif"],
                "body": ["Inter", "sans-serif"],
                "serif": ["Merriweather", "serif"],
            },
            backgroundImage: {
                "gradient-radial": "radial-gradient(var(--tw-gradient-stops))",
                "gradient-conic": "conic-gradient(from 180deg at 50% 50%, var(--tw-gradient-stops))",
            },
            borderRadius: {
                "DEFAULT": "0.375rem",
                "lg": "0.5rem",
                "xl": "0.75rem",
                "2xl": "1rem",
                "full": "9999px"
            },
        },
    },
    plugins: [
        require('@tailwindcss/typography'),
        require('@tailwindcss/forms'),
        require('@tailwindcss/container-queries'),
    ],
};
export default config;
